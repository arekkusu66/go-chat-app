version: "2"
sql:
  - engine: "postgresql"
    queries: "app/db/queries.sql"
    schema: "app/db/migrations"
    gen:
      go:
        package: "db"
        out: "app/db"
        emit_json_tags: true
        overrides:
          - column: "users.password"
            go_struct_tag: 'json:"-"'

          - column: "user_relations.kind"
            go_type:
              import: "gochat/app/types"
              package: "types"
              type: "UserRelation"

          - column: "user_chatrooms.kind"
            go_type:
              import: "gochat/app/types"
              package: "types"
              type: "ChatRoom"

          - column: "auth_verification.kind"
            go_type:
              import: "gochat/app/types"
              package: "types"
              type: "Verification"

          - column: "dms.status"
            go_type:
              import: "gochat/app/types"
              package: "types"
              type: "DM"